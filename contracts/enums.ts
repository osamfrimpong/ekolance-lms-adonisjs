export enum ProgrammeStatus {
  UPCOMING = 'upcoming',
  ONGOING = 'ongoing',
  ENDED = 'ended',
}

export enum ProgrammeApplicationStatus {
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
}

export enum ResourceType {
  VIDEO = 'video',
  DOCUMENT = 'document',
  REPOSITORY = 'repository',
  WEBSITE = 'website',
}

export enum UserType {
  ADMIN = 'admin',
  TUTOR = 'tutor',
  STUDENT = 'student',
}
